# Frontend Vite env example
# Set these in Vercel (Environment Variables) or create .env when testing locally

# Base API URL (trailing slash optional) e.g. https://alertup-backend.onrender.com/api
VITE_API_URL=

# PayPal client id for the frontend SDK (use production client-id in production)
VITE_PAYPAL_CLIENT_ID=
